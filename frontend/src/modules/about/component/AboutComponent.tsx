import * as React from 'react';

export class AboutComponent extends React.Component {
    render(): JSX.Element {
        return (
            <h2 className={"subtitle"}>
                About
            </h2>
        )
    }
}